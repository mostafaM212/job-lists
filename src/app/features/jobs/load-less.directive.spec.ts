import { LoadLessDirective } from './load-less.directive';

describe('LoadLessDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadLessDirective();
    expect(directive).toBeTruthy();
  });
});
